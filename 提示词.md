# 给 Trae 的实施提示词

你现在要在当前仓库中实施 MCP Tool 契约重构。先完整阅读并严格执行：

1. `docs/tool-refactor-design.md`（本次实施的唯一设计依据）
2. `skill-tool优化参考文档/规范指导/MCP-Tool编写规范速览.md`
3. `skill-tool优化参考文档/规范指导/Skill与Tool编写示例.md`
4. `skill-tool优化参考文档/服务端取消检查点.md`
5. 仓库内存在的 `AGENT.md`/`AGENTS.md`（如有）

不要只看本文摘要就开始改。设计文档与本文冲突时，以设计文档为准；如果设计文档与真实代码无法对应，先报告差异和建议，不要擅自改业务逻辑。

## 一、目标

把当前两个公开 MCP Tool 调整为三个职责明确的 Tool：

- `engineering_facts`
- `report_prepare`
- `report_finalize`

为三者建立准确的 Pydantic 输入/输出 Schema、统一 `{status, data, warnings}` 信封、真实协作式取消、正确的错误边界，并完成既有 UI、Skill、文档和测试的必要协议同步。

## 二、不可突破的红线

1. **禁止新增 `read_artifact`、`read_file` 或任何文件读取 Tool。**
2. **禁止新增用户可见或模型可见的 `run_id`。** 继续复用 `_artifact_dir()` 的时间戳 + UUID 内部唯一目录。
3. **禁止重做 UI。** 不改页面布局、组件结构、视觉样式和业务展示，只允许修改两个 UI 的结果归一化层、必要协议类型、mock 和构建产物。
4. 不改工程事实算法、报告章节算法、Schema 业务含义和报告内容规则。
5. 不恢复已删除的旧 runner、兼容包装或历史四阶段实现。业务核心只认：
   - `src/engineering_facts/core.py`
   - `src/report_generation/core.py`
6. 不删除文件，不执行 `git reset/rebase/push`，不覆盖无关的现有改动，不安装全局依赖。
7. 每个生产 Tool 调用入口继续使用 `make_host_client(ctx)`，不得改成全局/静态 client 或直接构造 client。

当前工作树可能已有未提交修改。动手前先检查 `git status` 和相关文件 diff；只修改设计文档列出的必要文件。若目标区域与现有修改冲突，停止并说明，不能覆盖用户已有工作。

## 三、实施前门禁

先验证真实 MCP Host 是否满足以下条件：

- UI 能完整收到 `ToolResult._meta.ui_payload`；
- `_meta.ui_payload` 不进入 Agent 模型上下文；
- 约 780 KB 工程事实和约 42 KB Markdown 不会被 Host 截断；
- Host Agent 与 MCP Server 共享受控文件系统，Agent 能用自身已有能力读取 `work_package.json`、写出 `work_results.json`。

如果无法完成验证或任一条件不满足，**暂停本轮代码改造并报告阻断**。不得用新增读取 Tool 补洞，也不得临时把旧字段塞回严格新信封。需要内联回退时，必须先另行评审并明确成功 `data` Schema。

## 四、实现要求

### 1. Tool 契约

- 保持直接业务参数，不增加外层 `inp`。
- `engineering_facts(source_location, construction_unit=None)`：`construction_unit` 空值按空字符串处理，不宣称会自动推断。
- `report_prepare(engineering_facts_uri, project_name=None)`：内部映射到现有 `operation="prepare"`。当前 `ReportContext` 只有 `project_name`，不得丢字段。
- `report_finalize(work_package_uri, work_results_uri=None)`：MCP 层不再公开 inline `work_results`；内部映射到 `operation="finalize"`。
- `report_prepare` 不绑定 UI；另外两个 Tool 保持对应既有 UI。

### 2. 输出与 UI 数据

- 为每个 Tool 分别定义成功/失败 Pydantic 模型，以 `status` 为判别字段；显式导出严格 `outputSchema`，禁止继续返回任意 `dict[str, Any]`。
- 成功时产物必填、`data.error=null`；失败时产物为 null、summary 为空对象、`data.error` 必填。Schema 必须拒绝状态与数据交叉组合。
- 可预期业务失败返回失败信封；未预期内部错误、权限/原子写失败记录服务端日志并抛 `ToolError`，不得泄露 traceback 或不必要的敏感绝对路径。
- 模型可见 `structuredContent` 只放产物引用、summary、warnings/error。
- 完整工程事实放 `_meta.ui_payload.engineering_facts`，完整 Markdown 放 `_meta.ui_payload.markdown_content`。
- UI 归一化层只白名单读取上述两个大字段，绝不能展开整个 `ui_payload` 覆盖结构化结果。页面组件不改。
- 不要手工编辑 dist；完成源码修改后按现有命令重新构建 UI 和 Host。

### 3. 取消与产物提交

- 两个核心 `execute()` 仅增加向后兼容的 `cancel_event: threading.Event | None = None` 和真实阶段检查点。
- 定义并透传 `OperationCancelled`；它必须写在核心所有宽泛 `except Exception` 之前，不能被包装成业务失败。
- MCP 层显式保存 `asyncio.to_thread()` 对应的 worker Task，并用 `asyncio.shield()` 等待。
- 收到客户端取消后：设置 Event，等待 worker 完成清理，再传播原始 `CancelledError`。不能只取消外层协程后放任线程继续。
- 工程事实、工作包在写正式文件前设置最后取消检查点，采用临时文件 + `os.replace`。
- DOCX 与 Markdown 必须先完整写入临时文件、校验、检查取消，再进入不可中断的分组提交段。只有两个正式文件都发布成功才返回 artifacts；系统错误不得返回半组结果。

### 4. fallback 语义

- `report_finalize.status="completed"` 只表示导出动作完成。
- `fallback_section_count == 0` 才可作为完整报告交付。
- `fallback_section_count > 0` 只能通过现有 summary/diagnostics 明确标注为“未闭合草稿”，继续补充工作结果；不要为此修改页面组件。

### 5. Skill 与文档

同步更新 `SKILL.md`、`skill.yaml`、`README.md` 中的 Tool 名称、调用顺序、状态闭环和错误处理。Skill 必须说明：Host Agent 使用自身文件系统能力读取工作包并写工作结果，而不是调用一个新的读取 Tool。

## 五、验证要求

修改后必须实际执行并记录结果：

1. `tools/list` 只出现三个目标 Tool，且没有任何读取 Tool。
2. 检查三个真实 `inputSchema`/`outputSchema`，包括 required、枚举、`additionalProperties`、成功/失败联合约束。
3. 覆盖三个 Tool 的正常路径、可预期失败、未预期异常、取消路径。
4. 取消测试必须等待后台 worker 退出，再检查没有本次运行的半成品和 `.tmp`。
5. 覆盖 DOCX/Markdown 分组提交、fallback 两种交付语义、并发运行目录隔离、错误信息不泄露。
6. 用真实 Host 验证 `_meta` 的 UI 可见性、模型不可见性和代表性数据不截断。
7. 完成 prepare → Host Agent 编制 → finalize 端到端链路，过程中不依赖读取 Tool。
8. 运行现有核心单元测试、MCP 契约测试、编译检查、Skill 校验、UI/Host 构建，并验证实际 dist 能运行。
9. 检查受保护页面组件、业务算法、Schema 业务规则没有被改动。

不得通过删除断言、跳过测试、吞异常或放宽 Schema 让测试通过。若环境缺少验证条件，要明确写“未验证”，不能声称通过。

## 六、交付格式

完成后按以下结构汇报：

1. 改了什么：按 Tool、取消、UI 适配、Skill/文档分类说明。
2. 修改文件：逐项列出，并说明为什么必要。
3. 契约结果：列出三个 Tool 的最终入参、状态和关键出参。
4. 验证证据：列出实际运行命令与结果；区分通过、失败、未执行。
5. 边界证明：明确说明没有新增读取 Tool、没有公开 `run_id`、没有修改 UI 页面与业务算法。
6. 遗留问题：只写真实未完成项或阻断，不自行扩展范围。

在全部验证完成前不要宣布完成，也不要提交或推送 Git。
